export class Developer extends Persone {  
    constructor (language, tocode) {
        super (name, age, gender);
        this.language = language;
        this.tocode = tocode;
    }
        great () {
            return ;
    }
}